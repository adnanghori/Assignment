/*
write a switch statement to select an operation based on the value of inventory .increment
total_ribbon by ribbon_order if inventory is ‘B’,or ’C’; increment total_label by label if inventory is ‘E’ ,’F’
or ‘D’ ;increment total_paper by paper_order if inventory is ‘A’ or ‘X’ Do nothing if inventory is ‘M’
.diosplay an error message if the value of inventory is not one of these eight letters.
*/
#include <stdio.h>
#include <ctype.h>
int main(int argc, char const *argv[]) {
    int totalRibbon,ribbonOrder,totalPaper,paperOrder,totalLabel,label;
    char inventory;
    scanf("%c",&inventory);
    switch (inventory) {
      
    }

  return 0;
}
